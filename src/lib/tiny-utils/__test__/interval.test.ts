import { interval } from "..";

test(`test fn interval`, async () => {
  // let i = 0;
  // const res: any = await new Promise((res) => {
  //   const callback = jest.fn((count) => {
  //     if (i === 9) {
  //       res({
  //         callback,
  //         i,
  //       })
  //     }
  //     i += 1
  //   })
  //   const clear = interval(callback, 100);
  // })
  // expect(res.callback.mock.calls).toHaveBeenCalledTimes(10);
  // expect(res.i).toHaveBeenCalledTimes(9);
});
